'use client';
import { useState } from 'react'; import Link from 'next/link'; import { supabase } from '../../../lib/supabase';
export default function NewResident(){const [form,setForm]=useState({building:'',apartment:'',first_name:'',last_name:'',phone:''});const [msg,setMsg]=useState('');const [busy,setBusy]=useState(false);async function save(e:any){e.preventDefault();setMsg('');setBusy(true);const full_name=`${form.first_name} ${form.last_name}`.trim();const {data:dup}=await supabase.from('residents').select('id').eq('building',form.building).eq('apartment',form.apartment).eq('full_name',full_name).limit(1);if(dup?.length){setMsg('يوجد جار مسجل بنفس البيانات.');setBusy(false);return}const {error}=await supabase.from('residents').insert({...form,full_name,updated_at:new Date().toISOString()});setMsg(error?`تعذر الحفظ: ${error.message}`:'تمت إضافة الجار بنجاح.');if(!error)setForm({building:'',apartment:'',first_name:'',last_name:'',phone:''});setBusy(false)}return <main className="wrap"><div className="sectionTitle"><h2>إضافة جار جديد</h2><Link href="/residents">العودة للجيران</Link></div><form className="card formGrid" onSubmit={save}><label>العمارة<input required value={form.building} onChange={e=>setForm({...form,building:e.target.value})}/></label><label>الشقة<input required value={form.apartment} onChange={e=>setForm({...form,apartment:e.target.value})}/></label><label>الاسم<input required value={form.first_name} onChange={e=>setForm({...form,first_name:e.target.value})}/></label><label>اللقب<input required value={form.last_name} onChange={e=>setForm({...form,last_name:e.target.value})}/></label><label>رقم الهاتف<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><div><button className="primary" disabled={busy}>{busy?'جاري الحفظ...':'حفظ الجار'}</button></div>{msg&&<div className="notice">{msg}</div>}</form></main>}
