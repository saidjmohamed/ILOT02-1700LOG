'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import { supabase } from '../../lib/supabase';
function normalizePhone(value:string){const v=value.replace(/[\s-]/g,'');if(v.startsWith('+213'))return v;if(v.startsWith('0')&&v.length===10)return '+213'+v.slice(1);return v}
export default function Login(){const router=useRouter();const[phone,setPhone]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError('');setBusy(true);const{error}=await supabase.auth.signInWithPassword({phone:normalizePhone(phone),password});if(error)setError('رقم الهاتف أو كلمة المرور غير صحيحة.');else router.replace('/');setBusy(false)}return <main className="authScreen"><form className="card authCard" onSubmit={submit}><div className="brandMark authMark">🏘️</div><h1>تسجيل الدخول</h1><p className="muted">نظام حي 1700 مسكن — إيلو 02</p><label>رقم الهاتف<input required inputMode="tel" autoComplete="tel" placeholder="0558357689" value={phone} onChange={e=>setPhone(e.target.value)}/></label><label>كلمة المرور<input required type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="notice">{error}</div>}<button className="primary" disabled={busy}>{busy?'جاري الدخول...':'دخول'}</button></form></main>}
