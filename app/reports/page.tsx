'use client';
import { useEffect, useState } from 'react'; import { supabase } from '../../lib/supabase';
export default function Reports(){const[summary,setSummary]=useState<any>(null),[treasuries,setTreasuries]=useState<any[]>([]),[contributions,setContributions]=useState<any[]>([]),[expenses,setExpenses]=useState<any[]>([]),[transfers,setTransfers]=useState<any[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');const money=(v:unknown)=>`${Number(v||0).toLocaleString('ar-DZ')} دج`;async function load(){setLoading(true);setError('');const{data:user}=await supabase.auth.getUser();if(!user.user){setError('يجب تسجيل الدخول لعرض التقرير المالي.');setLoading(false);return}const[s,t,c,e,tr]=await Promise.all([supabase.from('finance_summary').select('*').single(),supabase.from('treasury_summary').select('*').order('treasury_type').order('building'),supabase.from('contributions').select('id,amount,contribution_date,note,resident_id,treasury_id,residents(full_name,building,apartment),treasuries(name)').order('contribution_date',{ascending:false}),supabase.from('transactions').select('id,amount,transaction_date,title,description,treasuries(name),categories(name)').eq('kind','expense').order('transaction_date',{ascending:false}),supabase.from('treasury_transfers').select('id,amount,transfer_date,note,from_treasury_id,to_treasury_id,from:treasuries!treasury_transfers_from_treasury_id_fkey(name),to:treasuries!treasury_transfers_to_treasury_id_fkey(name)').order('transfer_date',{ascending:false})]);const first=s.error||t.error||c.error||e.error||tr.error;if(first)setError(first.message);setSummary(s.data||null);setTreasuries(t.data||[]);setContributions(c.data||[]);setExpenses(e.data||[]);setTransfers(tr.data||[]);setLoading(false)}useEffect(()=>{load()},[]);const monthly=contributions.reduce<Record<string,number>>((a,r)=>{const k=(r.contribution_date||'').slice(0,7)||'غير محدد';a[k]=(a[k]||0)+Number(r.amount||0);return a},{});const categories=expenses.reduce<Record<string,number>>((a,r)=>{const k=r.categories?.name||'بدون تصنيف';a[k]=(a[k]||0)+Number(r.amount||0);return a},{});if(loading)return <main className="wrap"><div className="card">جاري تحميل التقرير المالي...</div></main>;return <main className="wrap"><div className="sectionTitle"><div><h2>التقارير المالية</h2><p className="muted">تقرير مباشر للخزائن والمساهمات والمصاريف والتحويلات.</p></div><button className="secondary" onClick={load}>تحديث التقرير</button></div>{error&&<div className="notice">تعذر تحميل بعض بيانات التقرير: {error}</div>}<section className="stats"><div className="card"><div className="statLabel">إجمالي المساهمات</div><div className="statValue">{money(summary?.total_income)}</div></div><div className="card"><div className="statLabel">إجمالي المصاريف</div><div className="statValue">{money(summary?.total_expense)}</div></div><div className="card"><div className="statLabel">الرصيد الإجمالي</div><div className="statValue">{money(summary?.balance)}</div></div><div className="card"><div className="statLabel">عدد العمليات</div><div className="statValue">{summary?.transaction_count||0}</div></div></section><div className="card"><h3>تفصيل الخزائن</h3>{!treasuries.length?<div className="empty">لا توجد خزائن متاحة.</div>:<div className="tableWrap"><table><thead><tr><th>الخزينة</th><th>المستهدف</th><th>المساهمات</th><th>المصاريف</th><th>محول للخارج</th><th>وارد</th><th>الرصيد</th><th>الإنجاز</th></tr></thead><tbody>{treasuries.map(t=><tr key={t.id}><td>{t.name}</td><td>{money(t.target_amount)}</td><td>{money(t.total_contributions)}</td><td>{money(t.total_expenses)}</td><td>{money(t.total_transfers_out)}</td><td>{money(t.total_transfers_in)}</td><td>{money(t.balance)}</td><td>{Number(t.target_percent||0).toLocaleString('ar-DZ')}%</td></tr>)}</tbody></table></div>}</div><div className="card"><h3>التفصيل الكامل للمساهمات</h3>{!contributions.length?<div className="empty">لا توجد مساهمات مسجلة حسب صلاحيات حسابك.</div>:<div className="tableWrap"><table><thead><tr><th>الجار</th><th>العمارة</th><th>الشقة</th><th>الخزينة</th><th>المبلغ</th><th>التاريخ</th><th>ملاحظات</th></tr></thead><tbody>{contributions.map(c=><tr key={c.id}><td>{c.residents?.full_name||'جار غير محدد'}</td><td>{c.residents?.building||'—'}</td><td>{c.residents?.apartment||'—'}</td><td>{c.treasuries?.name||'—'}</td><td><b className="green">+{money(c.amount)}</b></td><td>{c.contribution_date||'—'}</td><td>{c.note||'—'}</td></tr>)}</tbody></table></div>}</div><div className="card"><h3>التحويلات بين الخزائن</h3>{!transfers.length?<div className="empty">لا توجد تحويلات مسجلة حسب صلاحيات حسابك.</div>:<div className="tableWrap"><table><thead><tr><th>من</th><th>إلى</th><th>المبلغ</th><th>التاريخ</th><th>ملاحظات</th></tr></thead><tbody>{transfers.map(x=><tr key={x.id}><td>{x.from?.name||'—'}</td><td>{x.to?.name||'—'}</td><td><b className="green">{money(x.amount)}</b></td><td>{x.transfer_date}</td><td>{x.note||'—'}</td></tr>)}</tbody></table></div>}</div><div className="card"><h3>المساهمات حسب الشهر</h3>{!Object.keys(monthly).length?<div className="empty">لا توجد مساهمات.</div>:Object.entries(monthly).sort(([a],[b])=>b.localeCompare(a)).map(([m,v])=><div className="tx" key={m}><span>{m}</span><b className="green">+{money(v)}</b></div>)}</div><div className="card"><h3>تفصيل المصاريف</h3>{!expenses.length?<div className="empty">لا توجد مصاريف مسجلة.</div>:<div className="tableWrap"><table><thead><tr><th>الخزينة</th><th>البيان</th><th>التصنيف</th><th>المبلغ</th><th>التاريخ</th></tr></thead><tbody>{expenses.map(e=><tr key={e.id}><td>{e.treasuries?.name||'—'}</td><td>{e.title}{e.description?` — ${e.description}`:''}</td><td>{e.categories?.name||'بدون تصنيف'}</td><td><b className="red">−{money(e.amount)}</b></td><td>{e.transaction_date||'—'}</td></tr>)}</tbody></table></div>}</div><div className="card"><h3>المصاريف حسب التصنيف</h3>{!Object.keys(categories).length?<div className="empty">لا توجد مصاريف.</div>:Object.entries(categories).map(([k,v])=><div className="tx" key={k}><span>{k}</span><b className="red">−{money(v)}</b></div>)}</div></main>}
