'use client';
import {useState} from 'react'; import Link from 'next/link'; import {supabase} from '../../../lib/supabase';
export default function NewTreasury(){const [f,setF]=useState({name:'',description:'',purpose:'',target_amount:''});const [msg,setMsg]=useState('');async function save(e:any){e.preventDefault();const {error}=await supabase.from('treasuries').insert({name:f.name,description:f.description||null,purpose:f.purpose||null,target_amount:Number(f.target_amount||0)});setMsg(error?`تعذر إنشاء الخزينة: ${error.message}`:'تم إنشاء الخزينة.');if(!error)setF({name:'',description:'',purpose:'',target_amount:''})}return <main className="wrap"><div className="sectionTitle"><h2>إضافة خزينة مالية</h2><Link href="/treasuries">العودة</Link></div><form className="card formGrid" onSubmit={save}><label>اسم الخزينة<input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>المبلغ المستهدف<input type="number" min="0" step="0.01" value={f.target_amount} onChange={e=>setF({...f,target_amount:e.target.value})}/></label><label>الهدف<textarea value={f.purpose} onChange={e=>setF({...f,purpose:e.target.value})}/></label><label>الوصف<textarea value={f.description} onChange={e=>setF({...f,description:e.target.value})}/></label><button className="primary">إنشاء الخزينة</button>{msg&&<div className="notice">{msg}</div>}</form></main>}
