'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import { supabase } from '../../lib/supabase';
export default function Login(){const router=useRouter();const[phone,setPhone]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError('');setBusy(true);const{data,error:fnError}=await supabase.functions.invoke('login-with-phone-username',{body:{phone,password}});if(fnError||!data?.access_token){setError('اسم المستخدم أو كلمة المرور غير صحيحة.');setBusy(false);return}const{error:setErrorResult}=await supabase.auth.setSession({access_token:data.access_token,refresh_token:data.refresh_token});if(setErrorResult){setError('تعذر إنشاء جلسة الدخول. حاول مرة أخرى.');setBusy(false);return}router.replace('/');setBusy(false)}return <main className="authScreen"><form className="card authCard" onSubmit={submit}><div className="brandMark authMark">🏘️</div><h1>تسجيل الدخول</h1><p className="muted">نظام حي 1700 مسكن — إيلو 02</p><label>اسم المستخدم (رقم الهاتف)<input required inputMode="tel" autoComplete="username" placeholder="0558357689" value={phone} onChange={e=>setPhone(e.target.value)}/></label><label>كلمة المرور<input required type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="notice">{error}</div>}<button className="primary" disabled={busy}>{busy?'جاري الدخول...':'دخول'}</button></form></main>}
